// @ts-nocheck
const crypto=require("crypto");class HashUtility{static hash(t,a="sha256"){if("string"!=typeof t)throw new Error("Data to hash must be a string.");return crypto.createHash(a).update(t).digest("hex")}static verify(t,a,r="sha256"){return HashUtility.hash(t,r)===a}}export default HashUtility;